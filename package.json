{
  "name": "react-with-less",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.19.0",
    "react": "16.8.6",
    "react-app-rewire-less": "^2.1.3",
    "react-app-rewired": "^1.6.2",
    "react-dom": "16.8.6",
    "react-router-dom": "^5.0.1",
    "react-scripts": "2.1.1",
    "react-test-renderer": "^16.8.6"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test",
    "eject": "react-scripts eject",
    "mock:install": "cd mock-server && yarn",
    "mock:start": "cd mock-server && yarn start",
    "start:all": "concurrently \"yarn mock:start\" \"yarn start\"",
    "install:all": "concurrently \"yarn\" \"yarn mock:install\""
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
